# Features & limitations — LamaSync

## What's implemented

|Component|Status|
|---|---|
|`@lamasync/core` — shared types, DB schema, TOML config, API client|done|
|`@lamasync/server` — REST + WebSocket + Swagger + auth|done|
|`@lamasync/daemon` — heartbeat, rclone execution, mounts, scheduler, socket server|done|
|`@lamasync/tui` — Single tabbed shell with 6 views + guided wizards + CLI fallback (LAMA-173)|done|
|`@lamasync/web-ui` — React SPA embedded in the server binary (Dashboard, Folders, Apps, Conflicts, Admin)|done|
|Agent skill (`lamasync` two-tier bundle: `SKILL.md` + `reference/`) — LAMA-230|done (+ installed)|
|Docker: `Dockerfile.server`, `docker-compose.yml`|done|
|`bun run build` → standalone binaries|working|
|Unit tests (core + server + daemon + self-update + TUI + executor + offset + web-ui + wizard)|passing|
|End-to-end smoke verification (health, register, folders, app snapshots, daemon, TUI, web UI)|done|

## Implemented features by issue

Rows describe their original delivery. LAMA-316 supersedes the older
dotfile/profile/manifests rows below; their removed paths are retained only as
historical context.

| Issue | Feature | Location |
|-------|---------|----------|
| LAMA-262 | Pair a device with a short code / QR — no API-key copy-paste; the exchange now mints a host-bound **device** key (LAMA-234): never the master key | `server/src/routes/pairing.ts`, `core/src/api-client.ts`, `tui/src/cli/register.ts`, `web-ui/src/components/PairingModal.tsx` |
| LAMA-234 | Managed API keys: master / admin / device credentials, device-key route allowlist + per-host ownership, key lifecycle routes (`/api-keys`, `/auth/me`), pairing mints host-bound device keys, Admin Access keys UI, **Access keys TUI drill-in under More** (masked table, create/pair/reveal/revoke/refresh, device-principal read-only identity screen, transient ack'd secret panels, pair code + countdown + poller) | `core/src/{types.ts,db/schema.ts,api-client.ts}`, `server/src/{api-keys.ts,auth.ts,ws.ts,routes/{api-keys,pairing,hosts,config,report,actions,operations,conflicts,restic,dotfiles,folders}.ts}`, `web-ui/src/{api.ts,access-keys.ts,components/AccessKeysPanel.tsx,pages/Admin.tsx}`, `tui/src/{access-keys.ts,views/access-keys.ts}`, `packages/agent-skill/reference/api.md` |
| LAMA-114 | Bandwidth scheduling (`--bwlimit`) | `daemon/src/executor.ts`, `folders.ts` |
| LAMA-116 | Disk-space pre-flight | `daemon/src/executor.ts` |
| LAMA-117 | Operation-log retention | `server/src/routes/admin.ts`, `server/src/index.ts` |
| LAMA-118 | WebSocket auth via subprotocol | `server/src/ws.ts`, `server/src/auth.ts`, `tui/src/views/fleet.ts` |
| LAMA-119 | Dry-run mode | `daemon/src/executor.ts` |
| LAMA-120 | `"git"` folder type | `daemon/src/executor.ts` |
| LAMA-104 | Error handling (core hardening) | `server/src/routes/report.ts`, `core/src/api-client.ts`, `server/src/index.ts`, `daemon/src/lock.ts`, `daemon/src/report-queue.ts`, `daemon/src/hooks.ts` |
| LAMA-162 | Automatic conflict strategies (`newer_wins`, `source_wins`, `keep_both`) | `core/src/types.ts`, `daemon/src/executor.ts` |
| LAMA-109 | App-specific "backup" dotfile system | `core/src/types.ts`, `core/src/db/schema.ts`, `server/src/routes/dotfiles.ts`, `server/src/routes/config.ts`, `daemon/src/executor.ts`, `tui/src/views/dotfiles.ts` |
| LAMA-316 | Application templates, protections, and snapshots (replaces dotfile/profile model) | `packages/core/src/{types.ts,db/schema.ts,api-client.ts}`, `packages/server/src/routes/apps.ts`, `packages/daemon/src/{executor.ts,scheduler.ts}`, `packages/web-ui/src/{api.ts,pages/{Presets,Dotfiles}.tsx}`, `packages/tui/src/cli/apps.ts` |
| LAMA-123 | LAN peer sync | `daemon/src/lan-peer.ts`, `server/src/routes/config.ts` |
| LAMA-124 | Encryption-at-rest | `server/src/routes/folders.ts`, `server/src/routes/config.ts` |
| LAMA-125 | `ARCHITECTURE.md` rewrite | `ARCHITECTURE.md` |
| LAMA-151 | Self-update (daemon + server release proxy) | `daemon/src/self-update.ts`, `server/src/routes/release.ts`, `packaging/install/update.sh` |
| LAMA-147 | Management Web UI (dashboard + admin CRUD) | `packages/web-ui/`, `packages/server/src/routes/web-ui.ts`, `scripts/inline-web-ui.ts` |
| LAMA-173 | TUI unification: tabbed shell, 6 views, guided wizards | `packages/tui/src/{boot.ts,index.ts,app/*,views/*,flows/*}` |
| LAMA-168 | Dotfile manifest improvements: excludes, host selector, cron presets + `@reboot`/`@login`, deployment tracking (last sync, direction, original uploader) | `core/src/{types.ts,db/schema.ts}`, `server/src/routes/{dotfiles.ts,config.ts,report.ts}`, `daemon/src/{executor.ts,scheduler.ts}`, `web-ui/src/pages/Dotfiles.tsx`, `tui/src/{flows/dotfile-manifest.ts,views/dotfiles.ts,app/schedule-presets.ts}` |
| LAMA-199 | Version & update visibility: daemon version in heartbeat, server-side `updateAvailable` vs cached GitHub release | `core/src/{types.ts,db/schema.ts,version-compare.ts}`, `server/src/{release-cache.ts,routes/{hosts.ts,health.ts,release.ts}}`, `daemon/src/index.ts`, `web-ui/src/pages/Dashboard.tsx` |
| LAMA-201 | Theme & design-token pass (dark/light) + inline SVG domain icons | `web-ui/src/{index.css,theme.ts,main.tsx,components/{Nav.tsx,icons.tsx}}` |
| LAMA-197 | Command Center dashboard v1 (landing page): needs-attention triage, fleet cards, live activity feed, quick actions | `web-ui/src/pages/Dashboard.tsx`, `web-ui/src/index.css` |
| LAMA-198 | Host list + detail pages; queued-action model (trigger_sync/backup, check_update, refresh_config) + config-revision auto-refresh | `core/src/{types.ts,db/schema.ts,api-client.ts}`, `server/src/{config-revision.ts,routes/{actions.ts,hosts.ts,config.ts,folders.ts,dotfiles.ts}}`, `daemon/src/{index.ts,actions.ts}`, `web-ui/src/pages/{Hosts.tsx,HostDetail.tsx}` |
| LAMA-200 | Notification foundation: ntfy + LamaDB webhook + durable event log + host-staleness sweep | `server/src/{notifications.ts,routes/notifications.ts}`, `web-ui/src/pages/Admin.tsx` |
| LAMA-202 | Read-only Data Browser (local dir, SigV4 S3 listing, restic metadata) | `server/src/{browse-paths.ts,s3-list.ts,routes/browse.ts}`, `web-ui/src/pages/DataBrowser.tsx` |
| LAMA-203 | "Since last visit" highlighting on Command Center | `web-ui/src/pages/Dashboard.tsx` |
| LAMA-221 | Notification channels config UI (ntfy + webhook, severity filters, test button, env seeding) | `server/src/{notifications.ts,routes/notifications.ts}`, `core/src/db/schema.ts`, `web-ui/src/pages/Admin.tsx` |
| LAMA-222 | Reusable S3 backends: credentials stored once (encrypted at rest), folders reference by backendId; legacy s3_* columns lifted+dropped | `core/src/{types.ts,db/schema.ts,api-client.ts}`, `server/src/{crypto.ts,backends.ts,routes/backends.ts,routes/{config,folders,browse}.ts}`, `web-ui/src/pages/{Backends,Folders}.tsx` |
| LAMA-223 | Tailnet IP surfacing: daemon detection + heartbeat, tailnet-first peer rclone config, UI columns/copy | `daemon/src/{lan-peer.ts,index.ts}`, `server/src/routes/{hosts,config}.ts`, `web-ui/src/pages/{Hosts,HostDetail,Dashboard}.tsx`, `tui/src/views/fleet.ts` |
| LAMA-224 | Storage stats: per-backend report + folder sizes (cached), Dashboard Storage card, Folders Size column | `core/src/types.ts`, `server/src/{stats.ts,routes/stats.ts,routes/folders.ts,routes/report.ts}`, `web-ui/src/pages/{Dashboard,Folders}.tsx` |
| LAMA-225 | Host rename: PATCH /hosts/:id (label-first, id stable), re-key cascade on re-registration, host_renamed WS + banner, inline edit | `server/src/routes/hosts.ts`, `core/src/api-client.ts`, `web-ui/src/{components/EditableHostname.tsx,pages/{Hosts,HostDetail}.tsx}`, `daemon/src/index.ts` |
| LAMA-226 | Data Browser write ops: copy/move/rename/mkdir/upload with jobs + progress + busy guard | `core/src/{types.ts,db/schema.ts}`, `server/src/{browse-jobs.ts,routes/browse.ts}`, `web-ui/src/pages/DataBrowser.tsx` |
| hidden-api-power | Per-folder "Sync now" + dry-run: daemon `runOnce` dryRun option, `trigger_sync` dryRun payload with `dry-run:` ack prefix, Folders/HostDetail enqueue buttons | `daemon/src/{index.ts,actions.ts}`, `web-ui/src/{pages/{Folders,HostDetail}.tsx,api.ts}` |
| hidden-api-power | Assignment editing: inline AssignmentEditor (path/role/schedule/conflict strategy + advanced), pause/resume toggle, PATCH route extended with role/localPath/bandwidthSchedule | `web-ui/src/{components/AssignmentEditor.tsx,pages/{HostDetail,Folders}.tsx}`, `server/src/routes/folders.ts` |
| hidden-api-power | Host delete/decommission UI (cascade confirm, navigate on success) | `web-ui/src/pages/{Hosts,HostDetail}.tsx`, `api.ts` |
| hidden-api-power | Dotfile versions on web: expandable per-app versions, download (auth-header blob fetch) + delete | `web-ui/src/{pages/Dotfiles.tsx,api.ts}` |
| hidden-api-power | Backend kinds `local`/`nfs`/`restic`: server-side path targets + centralized restic repo/password defaults, per-kind validation + all-kind test endpoint, rclone config gen, folder picker | `core/src/{types.ts,db/schema.ts}`, `server/src/{backends.ts,routes/{backends,config,folders}.ts}`, `web-ui/src/pages/{Backends,Folders}.tsx` |
| hidden-api-power | Conflicts history (Pending/Resolved/All tabs, name resolution, resolution record) + Operations active-locks panel + host filter | `web-ui/src/pages/{Conflicts,Operations}.tsx`, `index.css` |
| onboarding-pass | Concept glossary + hint components, first-run checklist on Dashboard, login hint, form/empty-state coaching, Swagger link | `web-ui/src/{concepts.ts,components/{Hint,GettingStarted,Login,Nav}.tsx,pages/{Dashboard,Folders,Dotfiles,Conflicts,Operations,HostDetail,Backends}.tsx,index.css}` |
| tui-foundations | TUI first-run setup flow (client.toml writer, no silent localhost/dev-key defaults), `?` help overlay + global key hints + per-view hotkey footers, friendlyError() at catch sites, Fleet live status (fleetService.start + socket error listeners), `q`-quit fix on Conflicts, Logs footer single-source, wizard validation (absolute localPath, cron sanity) + emoji cleanup | `tui/src/{api.ts,boot.ts,friendly-error.ts,flows/{setup,backup-setup}.ts,app/{shell,cron,fleet-service}.ts,views/{conflicts,logs,local,fleet,dotfiles}.ts}` |
| implemented | Per-folder sync history: Operations page folder filter + `?folderId=`/`?hostId=` deep links from Folders/HostDetail History buttons | `web-ui/src/pages/{Operations,Folders,HostDetail}.tsx`, `web-ui/src/api.ts` |
| implemented | Restic restore UI: restore modal + jobs panel (2s polling) on the DataBrowser restic tab | `web-ui/src/pages/DataBrowser.tsx`, `web-ui/src/api.ts` |
| implemented | Data Browser delete + download: job-based `POST /browse/delete` (deletefile/purge), base64 `POST /browse/download` (64 MiB cap, realpath containment) | `server/src/{browse-jobs.ts,routes/browse.ts}`, `web-ui/src/pages/DataBrowser.tsx` |
| LAMA-227 | Agent surface umbrella: CLI-first `lamasync` is the primary agent surface; the REST/WS API is the escape hatch. No MCP server or user accounts; master/admin/device credentials and six safety rules form the contract. | `packages/agent-skill/`, `packages/tui/src/cli/`, `packages/core/src/api-client.ts` |
| LAMA-229 | CLI v1: workflow subcommands in the `lamasync` binary (`status`, `folders list|create|assign`, `backends list|create|test`, `sync`, `ops list`, `doctor`, `local status|folders|ops|sync|sync-all|mount|unmount`); `--json` everywhere; exit codes 0/1/2/3/4; auth discovery flags > env > client.toml; credential masking (first 8 + last 4) | `packages/tui/src/cli/`, `packages/tui/src/index.ts`, `packages/core/src/api-client.ts` |
| LAMA-230 | Agent skill: two-tier bundle (`SKILL.md` + `reference/{cli,api,recipes,troubleshooting,safety}.md`), install pipeline (`~/.lamasync/install-state.json` persisted; default-on, opt-out), `lamasyncd --update skill` refresh, `packaging/build-skill-tarball.sh` ships `lamasync-skill-<version>.tar.gz` as a release asset, `scripts/check-skill-drift.ts` runs in CI | `packages/agent-skill/`, `packaging/{build-skill-tarball.sh,install/}`, `packages/daemon/src/{index.ts,skill-update.ts}`, `scripts/check-skill-drift.ts`, `.github/workflows/ci.yml` |
| LAMA-231 | CLI full CRUD coverage: `folders update|delete|unassign|assignments`, `dotfiles list|upload|download|manifests`, `conflicts list|resolve`, `snapshots list`, `restore`, `browse local|s3|restic|jobs`, `notifications list|channels`, `hosts list|rename`, `register`, `shares list`, `admin prune`. Destructive commands prompt on a TTY, require `--yes` non-interactively (safety rule 5). | `packages/tui/src/cli/{folders-ext,dotfiles,conflicts,snapshots,browse,notifications,hosts,admin,safety}.ts`, `packages/tui/src/cli/dispatch.ts`, `packages/core/src/api-client.ts` |
| implemented | Shared `Modal`/`ConfirmDialog`/`PromptDialog` replacing all window.prompt/confirm; prune + overwrite confirmations | `web-ui/src/components/Modal.tsx`, `web-ui/src/pages/{Admin,DataBrowser}.tsx` |
| implemented | Admin Server block: `GET /health` extended with serverVersion + dbSizeBytes, latest-release check with update badge | `server/src/routes/health.ts`, `server/src/db.ts`, `web-ui/src/pages/Admin.tsx` |
| implemented | Validation sweep: shared cron validator on Folders, backend endpoint/bucket/path checks, centralized ApiError envelope parsing; Dashboard storage error surfaced, Login remember-me, sync-note name resolution | `web-ui/src/{cron.ts,api.ts,pages/*}` |
| LAMA-238 | In-form backend connection test: `POST /backends/test` validates an unsaved backend config (write-only fields fall back to the stored secret via `backendId`), shared helpers with the per-id test, "Test connection" button + inline result in the Add/Edit backend form | `server/src/{backend-test.ts,routes/backends.ts}`, `core/src/api-client.ts`, `web-ui/src/{api.ts,pages/Backends.tsx}` |
| LAMA-235 | Host filter in the Folders view (Dotfiles-style scope selector: All hosts + each host; filters rows to folders with an assignment on the selected host) | `web-ui/src/pages/Folders.tsx` |
| LAMA-241 | Client-onboarding rough edges: socket `sync` accepts `folder` alias + returns `folder not found: X` instead of silent `started:true`; `POST /folders` defaults to the first existing backend (400 only when none exist) with the UI preselecting it; daemon warns "local path missing, waiting for first use" per assignment; `PUT/PATCH/DELETE /assignments/:id` return 405 pointing at `/folders/:folderId/assign/:hostId` | `daemon/src/{socket.ts,config.ts,index.ts}`, `server/src/routes/{folders.ts,actions.ts}`, `web-ui/src/pages/Folders.tsx` |
| LAMA-232 | Action reclaim: stale 'taken' actions reaped back to 'pending' inside `GET /actions/pending` (10 min threshold); `GET /actions/taken` returns a host's taken actions for daemon boot-time reclaim; daemon re-executes orphaned actions at boot + polls immediately | `server/src/routes/actions.ts`, `core/src/api-client.ts`, `daemon/src/index.ts` |
| LAMA-243 | Update-check throttle + server proxy: daemon routes release checks through the cached `GET /api/v1/release/latest` proxy instead of api.github.com directly (`LamaSyncApiClient.getLatestRelease()`); persisted 15-min cooldown (`~/.config/lamasync/update-state.json`) bounds crash-loop re-checks; generated systemd unit sets `StartLimitIntervalSec=300`/`StartLimitBurst=8`; optional `LAMASYNC_GITHUB_TOKEN` env var adds auth to the server's release fetch | `core/src/api-client.ts`, `daemon/src/{index.ts,update-check.ts,systemd.ts}`, `server/src/release-cache.ts` |
| LAMA-242 | `--help`/`-h` for `lamasyncd` + `lamasync-server` (usage strings + known-flag sets in new `usage.ts`); unknown flags print usage to stderr and exit 2 instead of silently booting the process | `daemon/src/{index.ts,usage.ts}`, `server/src/{index.ts,usage.ts}` |
| LAMA-239 | Per-host mount/sync override: new `folder_assignments.mode` (`inherit|sync|mount`, default inherit) + `effectiveFolderType` helper; daemon reconciles mounts vs cron syncs on refresh/boot; `switchToMount`/`switchToSync` become per-host mode setters; Mode dropdown in AssignmentEditor + effective-mode badge in Folders/HostDetail | `core/src/{types.ts,db/schema.ts,effective-type.ts}`, `daemon/src/{index.ts,scheduler.ts}`, `server/src/routes/{config.ts,folders.ts}`, `web-ui/src/{api.ts,components/AssignmentEditor.tsx,pages/{Folders,HostDetail}.tsx}` |
| LAMA-267 | Schedules as human sentences: raw cron hidden behind a collapsed "Advanced: custom cron" reveal; a "Next: …" sentence previews the next run wherever a schedule is set, computed client-side with the same `cron-parser` the daemon uses; web preset labels consolidated into one shared module | `web-ui/src/{schedule-presets.ts,next-run.ts,cron.ts,components/AssignmentEditor.tsx,pages/{Folders,Dotfiles}.tsx}`, `packages/tui/src/app/schedule-presets.ts` |
| LAMA-271 | Empty states that teach: shared `EmptyState` component (CSS-drawn glyph, no emoji/assets — title + one-sentence how + primary CTA + optional 3-step hint) wired into Folders/Devices/Storage/Activity/Data browser + Dashboard empty-fleet; CTAs reuse existing flows; TUI empty text reworded to the glossary | `web-ui/src/components/EmptyState.tsx`, `web-ui/src/pages/{Folders,Hosts,Backends,Operations,DataBrowser,Dashboard}.tsx`, `web-ui/src/index.css`, `tui/src/views/{local,dotfiles,logs}.ts` |
| LAMA-272 | Device cards, not host table: Devices page becomes a responsive card grid (device glyph, pulsing status dot + text never color-alone, "last backup …ago" from the operations feed, click → detail); device-first copy sweep; OS icon + storage-used noted as wire gaps (no new endpoint) | `web-ui/src/pages/{Hosts,HostDetail}.tsx`, `web-ui/src/{index.css,components/EditableHostname.tsx}`, `tui/src/views/logs.ts` |
| LAMA-270 | Command palette (cmd+k): fixed-overlay fuzzy palette over navigation + page CTAs, dependency-free scorer, keyboard-driven (⌘/Ctrl+K, arrows, Enter, Esc, Tab), ARIA combobox, navigation derives from the shared rail nav model, router-push deep-link safe, no layout shift, reduced-motion safe | `web-ui/src/{components/CommandPalette.tsx,fuzzy.ts,fuzzy.test.ts,components/Nav.tsx,App.tsx,index.css}` |
| LAMA-263 | App presets gallery (curated): new `/presets` page under the Apps nav group listing 6 hand-picked apps (VS Code, Neovim, Zsh, Firefox, Git config, tmux) with per-OS settings paths; "Backup" reuses the existing dotfile-folder + manifest verbs (no new server endpoints); per-app device counts from existing manifests; static TS catalog (`presets.ts`) is the seam for a future registry | `web-ui/src/{presets.ts,presets.test.ts,pages/Presets.tsx,api.ts,components/Nav.tsx}` |
| LAMA-291 | User-defined reusable app profiles: starter/profile gallery, emoji + color metadata, create/edit/customize, save an existing backup as a profile, explicit one-device apply with collision choices | `core/src/{types.ts,db/schema.ts,api-client.ts}`, `server/src/routes/{app-profiles,dotfiles,config}.ts`, `web-ui/src/{api.ts,pages/{Presets,Dotfiles}.tsx,index.css}`, `agent-skill/reference/api.md` |
| LAMA-264 | Demo mode + "Delete demo data": additive `demo` flag on 8 tables (schema + migrations) + new `/api/v1/demo` route (`GET` state, `POST /seed`, `DELETE` wipes flagged rows FK-safe and idempotent); seed creates 3 fake hosts, local demo backends on a server-side seed dir, an operation timeline, and a browsable snapshot; nothing schedules a real sync or touches a real backend; Dashboard banner + empty-fleet CTA | `core/src/{types.ts,db/schema.ts}`, `server/src/routes/demo.ts`, `web-ui/src/{api.ts,pages/Dashboard.tsx,index.css}`, `server/src/routes/demo.test.ts` |
| LAMA-283 | Skill-drift check is strict: CI runs `scripts/check-skill-drift.ts --strict` (undocumented routes/commands fail the build) + AGENTS.md policy line | `.github/workflows/ci.yml`, `AGENTS.md` |
| LAMA-258 | Human-sentence activity feed: Operations + Dashboard render glossary sentences (verb + folder + from device + to destination · relative time · status word; status never colour-alone); raw server summary kept on hover tooltip; shared `relative-time.ts` + `format-bytes.ts` extracted from duplicated per-page helpers | `web-ui/src/{operation-sentence.ts,operation-sentence.test.ts,relative-time.ts,format-bytes.ts,format-bytes.test.ts,components/OperationSentence.tsx,pages/{Operations,Dashboard,Hosts}.tsx}` |
| LAMA-269 | Storage as a picture: per-destination donut (folder composition) + growth sparkline on the Backends Storage column + a fleet donut on the Dashboard storage summary; additive `size_history` table + `GET /folders/sizes` (bulk) + `GET /stats/storage/history` (per-backend series); graceful "Not measured yet" for non-S3; no fake zeros (failed/unmeasured sizes are never recorded) | `core/src/{db/schema.ts}`, `server/src/{stats.ts,routes/{folders,stats}.ts,routes/stats.test.ts}`, `web-ui/src/{api.ts,components/{Donut,Sparkline}.tsx,components/{Donut,Sparkline}.test.ts,format-bytes.ts,pages/{Backends,Dashboard}.tsx,index.css}`, `packages/agent-skill/reference/api.md` |
| LAMA-282 | Host OS + storage-used on the wire: daemon reports `os` + `storageUsedBytes` on each heartbeat (additive optional body fields, `hosts.os`/`storage_used_bytes` in SCHEMA + MIGRATIONS); device cards show OS + "X used" | `core/src/{types.ts,db/schema.ts}`, `server/src/routes/hosts.ts`, `daemon/src/{device-info.ts,index.ts}`, `web-ui/src/pages/Hosts.tsx` |
| LAMA-257 | Dry-run preview drawer ("Preview next run" on device detail): enqueues `trigger_sync {dryRun:true}` (existing daemon `--dry-run` path), polls the action, reads the tagged `dry-run:` operation row, renders would-copy/delete/mkdir counts + capped file list; no new endpoints; dry-run rows never bump `config_revision` | `web-ui/src/{dry-run.ts,dry-run.test.ts,components/DryRunDrawer.tsx,pages/HostDetail.tsx,index.css}` |
| LAMA-268 | Smart conflict cards: Conflicts page is a card grid with this-device vs destination side-by-side (size + mtime) and Keep local/remote/both via the existing resolve verb; winner/loser always text labels; additive `localSizeBytes`/`remoteSizeBytes` (daemon stats local, remote stays null); `conflicts` gains `local_size`/`remote_size`/`demo` (SCHEMA + MIGRATIONS); demo seeds 2 pending conflicts, demo-delete cleans them | `core/src/{types.ts,db/schema.ts}`, `server/src/routes/{conflicts.ts,demo.ts}`, `daemon/src/executor.ts`, `web-ui/src/pages/Conflicts.tsx`, `web-ui/src/index.css` |
| LAMA-153 | TUI markdown tables + foldable sections: pure width-aware renderer (`markdown.ts`, golden-tested) — aligned fixed-width tables + `<details>` folds as [+] / [-] rows (open set drives expansion); wired into the adaptive help overlay + app-settings instructions; tab-bar layout untouched | `tui/src/{markdown.ts,markdown.test.ts,app/shell.ts,views/dotfiles.ts}` |
| LAMA-273 | TUI pause / slow-mode: shared API-client methods (`getPause/setPause/clearPause/setHostPause/clearHostPause`) plus a status-bar indicator ("⏸ paused 39m" / "🐢 slow 1M · 2h" / ASCII bracket fallback) driven by a 30 s poller, and a Ctrl+P wizard (`flows/pause.ts`) that walks Scope → Duration → Mode → (bwlimit?) → Confirm and reuses the existing WizardRunner + LAMA-173 Enter/Esc semantics; friendly-error translator handles the daemon's `sync skipped: paused until <iso>` refusal; minimal chrome — indicator rides alongside the default hint / transient status message in the single status line (LAMA-275 / LAMA-276) | `core/src/api-client.ts`, `tui/src/{pause.ts,pause.test.ts,app/{shell.ts,pause-service.ts,pause-service.test.ts,friendly-error.ts,friendly-error.test.ts},flows/pause.ts,boot.ts}`, `agent-skill/reference/api.md` (server routes documented by the server-side commit) |
| LAMA-273 | Pause / slow mode (server + daemon + web): additive `pause_state` table (schema + migrations); `/api/v1/pause` CRUD (`GET` overview with expired-row pruning, `POST` global/per-host set — past `until` rejected in favor of explicit `DELETE` resume), every set/clear bumps `config_revision`; `/config/:hostId` gains additive effective `pause` (host row wins over global); daemon scheduler skips runs while paused and executor refuses fresh runs; slow mode routes `--bwlimit` through the existing bandwidthSchedule plumbing; web gets a countdown banner (never color-alone) on Dashboard/HostDetail + Pause… modal (1h/4h/until-resume presets, pause-vs-slow + bwlimit). Live-verified on prod incl. revision bump 36→37→38 | `core/src/{types.ts,db/schema.ts}`, `server/src/routes/{pause.ts,config.ts}`, `daemon/src/{scheduler.ts,executor.ts,index.ts}`, `web-ui/src/{api.ts,pause.ts,hooks/usePause.ts,components/{PauseBanner,PauseControl}.tsx,pages/{Dashboard,HostDetail}.tsx}` |
| LAMA-266 | Backup health "Prove it" + fire drills: `POST /backends/:id/prove` restores one small file from the latest restic snapshot into a private tempdir (sha256 + size reported, tempdir always cleaned); `POST /backends/:id/drill` = liveness probe (`restic snapshots --json`, chosen over full repo check for runtime) + prove + `health_drills` audit row + operation_log entry (`host_id="_backup-health-drill"`) + notification via the LAMA-200 engine; monthly server-side schedule skips if last drill is recent (restart-safe); backends carry additive `lastProveAt/lastProveOk`; web adds Prove-it/fire-drill buttons, drill history table, and a Dashboard "Verified <t> ago" badge (30-day rule); non-restic backends get a documented 409; failures scrubbed per LAMA-226. Live-verified against a real restic repo on prod (byte-exact restore) | `core/src/{types.ts,db/schema.ts}`, `server/src/{health-drill.ts,routes/health-drill.ts,backends.ts,notifications.ts,index.ts}`, `web-ui/src/{api.ts,backup-health.ts,pages/{Backends,Dashboard}.tsx}`, `docker/Dockerfile.server` (restic in runtime image), `agent-skill/reference/api.md` |
| LAMA-259 | Time-travel browser: `GET /folders/:id/snapshots` + `/snapshots/:snapshotId/files?path=&limit=` browse restic snapshot contents read-only through the existing restic runner seam (`ls --json`, no temp dirs, 404-before-spawn access boundary); DataBrowser gains a History chip scrubber (roving-tabindex keyboard nav, snapshot-mode listing with always-visible caption + Back-to-live/Esc exit, read-only toolbar for backup folders). Boot regression from mixed route-param names caught and fixed same-day | `server/src/routes/snapshots.ts(+test)`, `web-ui/src/{api.ts,snapshot-history.ts(+test),pages/DataBrowser.tsx}`, `agent-skill/reference/api.md` |
| LAMA-265 | Delight pass: reusable inline-SVG llama glyph (no assets/emoji); once-per-milestone confetti gated by localStorage + prefers-reduced-motion with static fallback (milestones: `first-backup-seen` Dashboard, `first-preset-backup` Presets); llama slot wired only into the Dashboard empty-fleet state; browser-verified via CDP incl. reduced-motion path | `web-ui/src/components/{Llama,Confetti}.tsx(+test),EmptyState.tsx,pages/{Dashboard,Presets}.tsx,index.css,motion.ts` |
| LAMA-288 | Polish run 2: P-A web UX hardening — shared `useOverlayA11y` (focus trap/Esc/invoker focus-return incl. disabled-trigger bug fix) applied to Modal/DryRunDrawer/DataBrowser modals; centralized `motion.ts` reduced-motion gate; inline error captions with retry replacing silent dashes across six pages; <900px responsive fixes (browser-verified at 375px); glossary sweep. P-B cleanup — TUI dotfile diff preview before restore (`dotfiles-diff.ts`: tar -tzf vs disk sha256, NEW/CHANGED/SAME, capped 50 lines), renderer smoke tests behind `LAMASYNC_TUI_TEST_VIEWS=1`, `POST /api/v1/admin/export` op-log archival (.ndjson.gz, archive-then-delete, idempotent), dead `LAMASYNC_NTFY_URL` env path removed, `LAMASYNC_SOCKET_PATH` doc row | `web-ui/src/hooks/useOverlayA11y.ts,+motion.ts,InlineError.tsx,+7 pages/components`; `tui/src/dotfiles-diff.ts(+test),views/dotfiles.ts,app/shell.test.ts,views/local.test.ts`; `server/src/routes/admin.ts(+test),notifications.ts`; `core/src/{config.ts,types.ts}` |
| LAMA-290 | CLI split-by-surface config fallback (owner-approved): interactive bare `lamasync` keeps the localhost/dev-key default with the loud fake-key banner; any explicit subcommand without `client.toml` refuses fast — exit 3, stderr message "No client.toml found at <path> — run bare 'lamasync' once to connect to your fleet", `--json` gets `{reason:"no-config"}` (same exit code as auth-failure, distinct reason); `doctor`/`local`/`register` exempt; fires before any network attempt | `tui/src/cli/{dispatch.ts,doctor.ts,cli-fallback.test.ts,commands.test.ts}`, `agent-skill/reference/{cli.md,troubleshooting.md}` |
| LAMA-260 | File view inside synced folders: `POST /folders/:id/files` multipart upload (100 MB cap enforced pre/mid/post-stream, s3/local/nfs writable, restic/sftp 409, temp file always cleaned, scrubbed failures); DataBrowser gains image/text preview (256 KB text cap, first-byte sniffing for extension-less files, truncation note), per-file download, and an upload modal with client-side path validation (no absolute/`..`); hidden on read-only snapshot contexts | `server/src/routes/folder-files.ts(+test),core/{types.ts,api-client.ts}`, `web-ui/src/{api.ts,file-preview.ts(+test),upload-path.ts(+test),pages/DataBrowser.tsx}`, `agent-skill/reference/api.md` |
| LAMA-262 | Pair a device with code/QR: `pairing_sessions` table (schema + migrations, unambiguous alphabet codes `LAMA-XXXX-XXXX`, 30–3600s TTL); `POST /pairing` (admin) / `GET /pairing/:code` / `POST /pairing/:code/exchange` (auth-exempt, single-use, returns today's pre-shared key with a stable wire for future rotation); 5-min sweep; `lamasync register [--code] [--force]` writes client.toml and is exempt from the no-config refusal; web shows code + QR (qrcode-generator, the one deliberate dep) + countdown + claimed-state polling in Admin | `core/src/{types.ts,db/schema.ts,api-client.ts}`, `server/src/routes/pairing.ts(+test),auth.ts,index.ts`, `tui/src/cli/{register.ts(+test),dispatch.ts,hosts.ts}`, `web-ui/src/{api.ts,pairing.ts(+test),components/PairingModal.tsx,pages/Admin.tsx,package.json}`, `agent-skill/reference/{api.md,cli.md}` |
| LAMA-302 | Event-triggered sync for active local worktrees: opt-in, default-off per-assignment watch config (`watchEnabled`/`watchQuietSec` 10-300s default 30/`ignoreGitMetadata`/`respectGitignore`) on `FolderAssignment` + SCHEMA + MIGRATIONS + API create/PATCH validation; platform-neutral `folder-watch.ts` boundary (`FolderWatchFactory`/`WatchController` debounce single-flight state machine: one run per burst, at most one post-run follow-up, no bypass of `runOnce` lock/pause/retry/reporting); Linux inotify adapter (`fs.watch` recursive, dirs-after-start, `.git`/gitignore event filtering); `watch-control.ts` `WatchCoordinator` reconciles one watcher per eligible effective-`sync` assignment (mount/backup/disabled/missing-path skipped, git-worktree check for respectGitignore) on boot/refresh/shutdown; `respectGitignore` builds a deterministic Git-ignore rclone filter snapshot (`ignore` lib, nested/negation/info-exclude/global, re-prefixed) and forces safe `--resync` on snapshot change; operation `trigger` (`watch`/`schedule`/`manual`) persisted in `operation_log` + returned on `/operations`; CLI `folders assign`/`folders assign-update` flags; web-ui AssignmentEditor watch controls (only for effective sync); TUI local view `watching` badge; docs in `agent-skill/reference/{cli,api}.md`. TUI full watch-editing editor deferred (no full sync-assignment editor) — edit via CLI/web. Live soak on a busy Git fixture is the outstanding item | `core/src/{types.ts,folder-watch.ts(+test),folder-watch.test.ts,db/schema.ts(+test),index.ts,api-client.ts}`, `server/src/routes/{folders.ts(+test),config.ts,report.ts(+test),operations.ts}`, `daemon/src/{folder-watch.ts(+test,folder-watch-integration.test.ts),watch-control.ts(+test),gitignore.ts(+test),executor.ts,index.ts,package.json}`, `tui/src/{cli/folders.ts,cli/folders-ext.ts,cli/dispatch.ts,views/local.ts}`, `web-ui/src/components/AssignmentEditor.tsx`, `agent-skill/reference/{cli.md,api.md}` |
| LAMA-299 | Remote daemon update from Host Detail: new `update_daemon` queued action (admin-only, NO payload — enqueues 400 on any; daemon targets the release proxy and picks its own asset, never `LAMASYNC_UPDATE_ASSET`); injected `performDaemonUpdate` helper in daemon (preflight server URL/credential → `/auth/me` + release-proxy reachability → restart-capability + writable resolved-binary check → release → strict asset selection → atomic replace) shared with the refactored `--update` CLI; dispatcher acks `"installed vX; service restart requested"` BEFORE restarting `lamasyncd.service` (fixed literal unit name), restart failure re-acks with the manual command; systemd-unavailable preflight fails with the manual path; outcomes name phase + version only, all error strings scrubbed (tests assert no key leak per outcome); web Host Detail gains a Software section (installed/latest/version-capability state machine `daemonUpdateUiState` — offline/no-version/daemon-too-old/no-update/ready/in-flight, min version `REMOTE_DAEMON_UPDATE_MIN_VERSION` 0.3.6 in core), confirmation dialog, duplicate-pending guard, follow-up status (queued → claimed → installed-awaiting-heartbeat → updated) with 5s poll + WS action-event refresh; skill/API docs updated | `core/src/{types.ts,remote-update.ts(+subpath export),api-client.ts}`, `server/src/routes/{actions.ts(+test)}`, `daemon/src/{daemon-update.ts(+test),actions.ts,index.ts,systemd.ts}`, `web-ui/src/{daemon-update.ts(+test),pages/HostDetail.tsx,api.ts}`, `agent-skill/reference/api.md` |
| LAMA-301 | Manual production server deploy control: `server_deploy_jobs` table (SCHEMA + MIGRATIONS, short history trimmed on create, never touched by op-log retention); admin-only route family `GET /server-deploys/config`, `POST/GET /server-deploys`, `GET /server-deploys/:id` plus deploy-agent-only `GET /server-deploys/pending`, `POST /server-deploys/:id/claim` (atomic pending→running, 409 on re-claim), `/:id/progress`, `/:id/complete`; hard security boundary — the server container gets no docker socket/SSH/shell endpoint; the LXC-resident `lamasync-deploy-agent` (new workspace package + `packaging/deploy-agent` systemd unit) runs the FIXED `/home/messhias/lamasync/update.sh` with no arguments and a fixed workdir, 10-min script timeout, incremental scrubbed output (shared `core/scrub.ts` scrubber + 16 KiB tail cap, applied agent- AND server-side), bounded exponential-backoff health wait before completion, completion retried through the API's own restart, stale running jobs (15 min) reclaimed to pending; new `deploy` ApiKeyKind/principal strictly limited to the agent routes; feature gate `LAMASYNC_DEPLOY_AGENT_ENABLED` (unconfigured → "manual deploy only"); Admin Server-deployment card (status/stage/collapsible sanitized tail, WS `server_deploy` events + 5s poll, duplicate-click coalescing server-side, retry only after terminal, failed links to `docs/prod-deploy.md` rollback); provisioning/rotation/uninstall documented in `docs/prod-deploy.md`. Disposable-host deploy smoke is the operator follow-up | `core/src/{types.ts,scrub.ts,db/schema.ts,api-client.ts}`, `server/src/{routes/server-deploys.ts(+test),deploy-scrub→core,auth.ts,index.ts,routes/api-keys.ts}`, `deploy-agent/src/{index.ts,runner.ts(+test)}`, `packaging/deploy-agent/lamasync-deploy-agent.service`, `web-ui/src/{api.ts,server-deploy-ui.ts(+test),pages/Admin.tsx}`, `docs/prod-deploy.md`, `agent-skill/reference/api.md` |

| LAMA-304 | Per-prefix S3 folder sizes: `GET /folders/:id/size` + `/folders/sizes` now measure a folder's size against its destination prefix (`rclone size --json stats:<bucket>/<prefix>`) instead of the whole bucket — folders sharing a bucket no longer each report the full bucket size; the prefix is resolved from the folder's assignments via core `resolveDestination` (explicit `destination`, else `<folder-name>` / `<folder-name>/<host-id>`); restic-repo assignments are excluded (not measurable as a prefix); the backend-level storage report (`/stats/storage`) stays bucket-level | `core/src/types.ts`, `server/src/stats.ts` |
| LAMA-305 | Read-only mount in initial device setup: the Folders page "Set up on device…" form offers the LAMA-239 per-device Mode override (Use folder default / Sync / Read-only mount) at assignment-creation time for sync and mount folders — same labels and copy as AssignmentEditor; hidden for backup/app-settings/git; `mode` rides the existing `POST /folders/:id/assign` payload (no wire change) | `web-ui/src/pages/Folders.tsx` |

## Known limitations

### Server
- **User accounts / OAuth** — credentials are still not user-bound. Managed
  master/admin/device keys provide operational separation, but multi-user
  identity and lifecycle would need a separate account/session model.

### TUI
- **Remote conflict sizes** — conflict cards show the destination size as `—`
  until a per-conflict remote stat is added; local sizes are available.

### Application capture portability (LAMA-316)
- **Linux/GNU tar verified only** — portable app archive-member rewriting uses
  GNU tar's `--transform` behavior. No macOS or Windows clients exist in the
  fleet yet; qualify their archive tooling before claiming app-capture support
  on either platform.

### LAN peer tailnet preference (LAMA-223)
- **Stale `tailnet_ip` never clears** — when a daemon's tailscale interface goes down, the field stays at its last value until the next successful detection. With tailnet preferred in peer rclone configs (P1-4), a stale address can strand peers — the daemons probe both tailnet and LAN sections before declaring the peer unreachable, so the worst case is a 5s sync delay, not a failure.

### Host rename hijack window (LAMA-225)
- The PATCH-then-reregister rename model (label-only PATCH, id re-key on re-registration) has a brief window where any machine claiming the new hostname absorbs the host identity (history, folders, dotfiles). The single-API-key trust model accepts this; the alternative would be a signed re-registration handshake. Acceptable for a personal sync fleet; document it for any multi-tenant rollout.

### Infrastructure
- **Windows/WSL support** — paths hardcoded to Unix conventions. rclone works on Windows but the daemon does not.
- **Server-side in-place self-update** — Docker/CI/CD is the path for the server; a live binary replacement path for the server is not built (the daemon/TUI self-update exists, LAMA-151).

## Future ideas (not planned)
- Health dashboard with predictive transfer-throughput alerts.
- Preset manifest packs (`dev-node`, `dev-python`, `omp`).
- `nix` folder type that triggers `home-manager switch` after sync.
- Git-folder sync with ahead/behind and dirty-worktree reporting.
