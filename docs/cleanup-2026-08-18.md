# Doc + polish cleanup — 2026-08-18

**Scope.** Twenty-five small items from the 2026-08-18 docs-overview pass.
Mostly <30 LOC each; pick from the top. The companion Multica issue is
**LAMA-247** (umbrella). This doc is the source of truth for scope; the
issue is the tracker.

**Out of scope.** Anything with its own Multica issue (LAMA-110, LAMA-204,
LAMA-228, LAMA-236, LAMA-237, LAMA-243) is already tracked separately.

**Conventions.**
- One commit per item (or grouped by file when two items are right next to
  each other). Keep the diffs small and focused.
- Run `bun x tsc --noEmit` after each batch.
- Tests added for behaviour changes; same-package tests, no new harness.
- Don't open follow-up issues from this doc — file them in Multica directly
  if the item turns out to be bigger than "no-issue".

**When done.** Delete this file and remove the LAMA-247 reference from
`docs/status.md` "Next session options".

---

## Priority order (top is most valuable)

### P0 — Doc refresh (no code change)

1. **`docs/development.md` test list** — already refreshed in
   `docs/cleanup-2026-08-18.md` commit. Verify by reading the file.
2. **`docs/status.md` "Open Multica issues" line** — already refreshed
   2026-08-18.
3. **`docs/prod-deploy.md` stale `/root/lamasync/` references** — only the
   historical note at line 27 remains; that's intentional.

### P1 — Server no-issue nits

4. **S3 download buffers the object before the 64 MiB cap check.**
   WS4 leftover. `POST /api/v1/browse/download` reads the whole remote into
   memory then validates size; OOM-prone for buckets > available heap.
   - Stream in chunks (range request / `rclone cat --use-json-log`); reject
     mid-stream when the size crosses the cap.
   - File: `packages/server/src/routes/browse.ts` (download handler).
   - Add a unit test that mounts a mock S3 object > 64 MiB and asserts the
     request fails fast without buffering.

5. **S3 missing-key returns 400 instead of the Swagger-advertised 404.**
   WS4 leftover. `400 "no such file"` should be `404` with the same body
   shape.
   - File: `packages/server/src/routes/browse.ts` (s3 handler).
   - Mirror the local-404 path.

6. **Operation-log archival.** Known limitation. Add a
   `POST /api/v1/admin/export?olderThanMs=…` that writes the operation_log
   to a tarball in the backup dir before deletion.
   - Files: `packages/server/src/routes/admin.ts`, `packages/server/src/stats.ts`-style
     helper.
   - Add a `--target` flag to `lamasync admin prune` that calls export
     first.

7. **Ntfy `ntfyUrl` is still unused.** Known limitation. The LAMA-200
   notification engine handles ntfy, but the legacy `LAMASYNC_NTFY_URL`
   env var hookup is dead. Either delete or wire it (delete is cheaper).
   - File: `packages/server/src/notifications.ts` (search for `ntfyUrl`),
     `docs/development.md` env table.

8. **Stale `tailnet_ip` never clears.** Known limitation. When tailscale
   goes down the field sticks at its last value. Make the daemon reset it
   on detection failure after a 5-min grace period.
   - Files: `packages/daemon/src/lan-peer.ts`, `packages/server/src/routes/hosts.ts`.

### P2 — Web UI nits

9. **Admin Server block swallows `/health` failure into `—` placeholders.**
   WS4 leftover. Make the error visible (small caption under the block).
   - File: `packages/web-ui/src/pages/Admin.tsx`.

10. **Web cron validator accepts `@noon` / `@yearly` / `@hourly` etc.** that
    the daemon can't schedule. WS4 leftover. Cross-check the preset
    allowlist against the daemon's `cron-parser` and reject the rest.
    - Files: `packages/web-ui/src/cron.ts`, `packages/web-ui/src/pages/Folders.tsx`,
      `packages/web-ui/src/pages/Dotfiles.tsx`.

11. **Rename-with-unchanged-name silently no-ops.** WS4 leftover. Either
    reject with 400 or return a success that does nothing explicitly.
    - File: `packages/server/src/routes/hosts.ts` (PATCH hostname).

12. **Backup-operation summaries misreport `0 transfers, 0 B` even when
    data copied.** Open gotcha (lamasync-client.md). The rclone JSON-log
    parsing for the summary is wrong.
    - File: `packages/daemon/src/executor.ts` (runOnce rclone parser).
    - Add a fixture: a small rclone `--log-file` JSONL sample with non-zero
      transfers and assert the summary reflects them.

### P3 — TUI / CLI no-issue nits

13. **CLI fallback silently defaults to `localhost/dev-key` when no
    `client.toml` exists.** WS6 watch item — parked. Decide whether
    `lamasync` should refuse to run with a fake key (safer) or keep the
    current friendly-default UX with a louder "this is fake, not your
    fleet" warning.
    - Files: `packages/tui/src/cli-fallback.ts`, `packages/tui/src/cli/client.ts`,
      `packages/tui/src/cli/doctor.ts`.

14. **`lamasync <command>` exit-3 should be greppable from `--json`
    output.** Currently the masked API key is in the JSON; some users want
    a structured `{ reason: "auth-failure" }` field instead.
    - Files: `packages/tui/src/cli/output.ts`, `packages/tui/src/cli/dispatch.ts`.

15. **Dotfile diff preview against current disk files before extraction.**
    Known limitation. TUI Restore flow is the easy place to start.
    - File: `packages/tui/src/views/dotfiles.ts`.
    - Use `Bun.spawn` to `tar -tzf` the tarball and compare against
      `find <extract-dir>`. Show a unified diff.

16. **Renderer smoke tests behind `LAMASYNC_TUI_TEST_VIEWS`.** Status.md
    polish. Foundation wired; add a couple of view-shape tests to lock the
    contract.
    - File: `packages/tui/src/views/*.test.ts` (new), gated on
      `process.env.LAMASYNC_TUI_TEST_VIEWS === "1"`.

### P4 — Daemon no-issue nits

17. **`LAMASYNC_SOCKET_PATH` isn't documented in
    `packages/agent-skill/lamasync-client.md`.** The dev env-var table in
    `docs/development.md` lists it, but the client skill doesn't. Add a
    one-liner to "Gotchas".
    - File: `packages/agent-skill/lamasync-client.md`.

18. **`update-check.test.ts` was added in LAMA-243.** Confirm
    `update-check.ts` tolerates drift between the persisted `lastCheckAt`
    and the daemon's local clock (currently OK because we use `Date.now()`,
    but worth a test that fakes the clock).
    - File: `packages/daemon/src/update-check.test.ts`.

### P5 — CI / docs

19. **`scripts/check-skill-drift.ts` — the `lamasync dotfiles manifests`
    regression in LAMA-240 exposed a deeper gap.** The CLI block in
    `reference/cli.md` is currently hand-maintained, with the drift-check
    catching only what `--help` sees. A
    `bun run scripts/gen-cli-reference.ts > reference/cli.md` script is
    mentioned in the doc but doesn't exist. Write the script (or remove
    the doc claim).
    - Files: `scripts/gen-cli-reference.ts` (new), `packages/agent-skill/reference/cli.md`.
    - Use `dumpCliHelp()` from `packages/tui/src/cli/dispatch.ts` (already
      exported, per LAMA-240) as the source.

20. **`.pi-subagents/` is in `.gitignore` but artifacts inside accumulate.**
    Add a `bun run clean:pi` script to nuke it.
    - Files: `package.json` (new script), `scripts/clean-pi-artifacts.ts` (new).

21. **`docs/features.md` "Future ideas" section** is a parking lot:
    predictive throughput, preset manifest packs, `nix` folder type,
    git-folder ahead/behind. None of these need issues but they're worth a
    once-a-year review. No-op for now; just a note.

### P6 — Documentation housekeeping

22. **`.memsearch/memory/` files are 6 dated scratch notes** — the audit,
    the workstreams, the agent-surface ceremony. They're not in
    `.gitignore` despite the index being excluded. Decide whether to
    commit them (referenced from status.md) or fully ignore.
    - Files: `.gitignore` (add `.memsearch/memory/`), or
      `git add` the meaningful ones.

23. **`docs/features.md` "Known limitations" / "Future ideas".** Mark the
    items covered by the items above as "tracked" with a link to the
    issue. No-op if you don't want a wiki.

24. **The six `docs/handoff` screenshots still in git history** — the
    deletion commit kept them out of the working tree, but the
    `dag` is still there. No action needed; just a note for the curious.

25. **`docs/architecture.md` doesn't mention LAMA-227/230/239/242/243.**
    Verify it's still consistent with the codebase after this cleanup.
    - File: `ARCHITECTURE.md`.

---

## Acceptance

- All 25 items resolved, or explicitly punted to a new Multica issue.
- `bun x tsc --noEmit` clean throughout.
- `bun test` passes at the end (target: 590+ pass / 1 skip / 0 fail).
- One line per item in the commit log so the work doc can be updated by
  ticking items off in the same PR.
- `docs/status.md` "Next session options" updated to remove the LAMA-247
  reference when the umbrella closes.
- This file (`docs/cleanup-2026-08-18.md`) deleted at the end.
