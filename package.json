{
  "name": "lamasync",
  "version": "0.2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun run --watch packages/server/src/index.ts",
    "dev:daemon": "bun run packages/daemon/src/index.ts",
    "dev:tui": "bun run packages/tui/src/index.ts",
    "build": "bun scripts/gen-version.ts && bun run --filter='*' build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
