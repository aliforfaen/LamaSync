<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night counterpart of values/themes.xml (see the comment there). -->
    <style name="Theme.LamaSync" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/lamasync_canvas_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
