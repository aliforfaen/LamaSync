name: ci

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:
    branches: [master]

permissions:
  contents: write
  packages: write

jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build web UI for server tests
        run: bun run --filter @lamasync/web-ui build

      - name: Typecheck
        run: bun x tsc --noEmit

      - name: Test
        run: bun test

      - name: Drift-check the agent skill (LAMA-230)
        run: bun run scripts/check-skill-drift.ts

  build:
    name: build
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: Build the agent-skill bundle (LAMA-230)
        run: bash packaging/build-skill-tarball.sh dist

      - name: Upload server binary
        uses: actions/upload-artifact@v4
        with:
          name: lamasync-server
          path: packages/server/dist/lamasync-server
          if-no-files-found: error

      - name: Upload daemon binary
        uses: actions/upload-artifact@v4
        with:
          name: lamasyncd
          path: packages/daemon/dist/lamasyncd
          if-no-files-found: error

      - name: Upload tui binary
        uses: actions/upload-artifact@v4
        with:
          name: lamasync-tui
          path: packages/tui/dist/lamasync-tui
          if-no-files-found: error

      - name: Upload skill bundle (LAMA-230)
        uses: actions/upload-artifact@v4
        with:
          name: lamasync-skill
          path: dist/lamasync-skill-*.tar.gz
          if-no-files-found: error

  release:
    name: release
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v5

      - name: Download server binary
        uses: actions/download-artifact@v4
        with:
          name: lamasync-server
          path: dist/lamasync-server

      - name: Download daemon binary
        uses: actions/download-artifact@v4
        with:
          name: lamasyncd
          path: dist/lamasyncd

      - name: Download tui binary
        uses: actions/download-artifact@v4
        with:
          name: lamasync-tui
          path: dist/lamasync-tui

      - name: Download skill bundle (LAMA-230)
        uses: actions/download-artifact@v4
        with:
          name: lamasync-skill
          path: dist/lamasync-skill

      - name: Make binaries executable
        run: chmod +x dist/lamasync-server/lamasync-server dist/lamasyncd/lamasyncd dist/lamasync-tui/lamasync-tui dist/lamasync-skill/lamasync-skill-*.tar.gz

      - name: Stage updater script
        run: cp packaging/install/update.sh dist/update.sh

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/lamasync-server/lamasync-server
            dist/lamasyncd/lamasyncd
            dist/lamasync-tui/lamasync-tui
            dist/lamasync-skill/lamasync-skill-*.tar.gz
            dist/update.sh
          generate_release_notes: true

  docker:
    name: docker
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/v'))
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Docker tags
        id: tags
        run: |
          set -euo pipefail
          REPO="${{ github.repository_owner }}"
          TAGS="ghcr.io/${REPO}/lamasync-server:latest"
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAGS="${TAGS}"$'\n'"ghcr.io/${REPO}/lamasync-server:${GITHUB_REF#refs/tags/}"
          fi
          {
            echo "tags<<EOF"
            echo "$TAGS"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.server
          push: true
          tags: ${{ steps.tags.outputs.tags }}
